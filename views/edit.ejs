<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <title>Edit Task Name</title>
</head>
<body>
  <div class="main w-full min-h-screen bg-zinc-900 flex flex-col items-center text-white">
    <a href="/" class="text-blue-500 inline-block mt-10 mb-10">← Go Back To Home</a>

    <div class="form p-10 w-full max-w-md bg-zinc-800 rounded-lg shadow-md space-y-6">
      <h1 class="text-2xl font-semibold text-center">Edit Task Name</h1>

      <form action="/edit/<%= filename %>" method="POST" class="space-y-5">

        <!-- OLD NAME (Visible but read-only) -->
        <div>
          <label class="block text-gray-300 mb-2">Current Task Name:</label>
          <input
            type="text"
            value="<%= filename %>"
            readonly
            class="block w-full rounded-md px-5 py-3 bg-zinc-700 text-gray-400 cursor-not-allowed"
          >
          <!-- Hidden field for backend -->
          <input type="hidden" name="Previous" value="<%= filename %>">
        </div>

        <!-- NEW NAME INPUT -->
        <div>
          <label class="block text-gray-300 mb-2">Enter New Task Name:</label>
          <input
            name="New"
            class="block w-full rounded-md px-5 py-3 bg-zinc-700 text-white placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-yellow-500"
            type="text"
            placeholder="Type new file name..."
            required
          >
        </div>

        <!-- SUBMIT BUTTON -->
        <input
          class="mt-4 w-full bg-yellow-600 hover:bg-yellow-700 text-white font-semibold py-3 rounded-md cursor-pointer transition duration-200"
          type="submit"
          value="Update Name"
        >
      </form>
    </div>
  </div>
</body>
</html>
